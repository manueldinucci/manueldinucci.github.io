# V11 acceptance

- `Presi · Rimasti · Totali` removed with no placeholder row.
- Demand line enlarged while retaining the v10 demand logic.
- `Lettera` renamed to `Lettera iniz.`.
- Free-player `+` uses graphite (`#3A3A3C` light, `#4A4A4D` dark); assigned `−` stays visually secondary.
- Secondary metadata/comment wraps and remains fully readable; no ellipsis/nowrap is applied to `.player-secondary-meta`.
- IndexedDB schema and auction/import logic unchanged.
- Service Worker cache bumped to v11.
