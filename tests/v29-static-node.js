const fs=require('fs'), path=require('path'), assert=require('assert');
const root=path.join(__dirname,'..');
const html=fs.readFileSync(path.join(root,'index.html'),'utf8');
const css=fs.readFileSync(path.join(root,'style.css'),'utf8');
const app=fs.readFileSync(path.join(root,'app.js'),'utf8');
const sw=fs.readFileSync(path.join(root,'service-worker.js'),'utf8');
const m = sw.match(/fantacalcio-checklist-v(\d+)(?:\.(\d+))?/);
assert(m && Number(m[1]) >= 29);
assert(html.includes('id="sortSheet" class="context-popover sort-popover hidden"'));
assert(html.includes('id="filtersPanel" class="context-popover filters-popover hidden"'));
assert(html.includes('aria-controls="sortSheet"'));
assert(html.includes('aria-controls="filtersPanel"'));
assert(app.includes("toggleContextPopover('sortSheet', 'sortBtn')"));
assert(app.includes("toggleContextPopover('filtersPanel', 'filtersBtn')"));
assert(app.includes('function positionContextPopover'));
assert(app.includes('function closeContextPopovers'));
assert(app.includes("window.addEventListener('scroll', () => closeContextPopovers()"));
assert(app.includes('const teams = [...new Set(state.players.map(p => p.squadra).filter(Boolean))]'));
assert(app.includes('const slots = [...new Set(state.players.map(p => p.slot).filter(Boolean))]'));
const roleTabBlock=app.slice(app.indexOf('function renderRoleTabs'), app.indexOf('function renderMainView'));
assert(!roleTabBlock.includes("state.team = ''"));
assert(!roleTabBlock.includes("state.slot = ''"));
assert(css.includes('/* v29 — popover contestuali Ordina/Filtri e filtri globali Squadra/Slot */'));
assert(css.includes('.context-popover'));
assert(css.includes('position: fixed'));
console.log('v29 static checks passed');
