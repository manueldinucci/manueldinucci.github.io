# V18 acceptance

- Top navigation uses Por / Dif / Cen / Att / Live / Rose on one row.
- Live ordering supports Slot rimasti and Max bid possibile, descending.
- Role badge uses `P/D/C/A - X SLOT RIMASTI` (singular handled) and `AL COMPLETO` at zero.
- Completed-role manager cards are visually attenuated but remain interactive.
- Expanded Live purchases are compact and split into two balanced columns, preserving name + acquisition price.
- Rose uses the same two-column compaction inside each role.
- Rose summary omits the redundant word `Rosa`.
- Service worker cache is v18.
