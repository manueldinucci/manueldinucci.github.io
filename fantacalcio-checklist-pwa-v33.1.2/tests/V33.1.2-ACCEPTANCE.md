# v33.1.2 — Acceptance

- Mappa Slot: `oneCreditBuy=true` mostra un micro-indicatore `(1)` secondario e non a badge; il marker è incluso anche nella misura del packing inline.
- Barra monitoraggio: ordine `★ | S1 | S2 | S3`, con S4/S5 rimossi soltanto dalla barra.
- `★` conta i Preferiti ancora liberi del ruolo, indipendentemente dai filtri visivi.
- Alert rosso solo con `posti_liberi > 0 && preferiti_liberi < posti_liberi`; uguaglianza e ruolo completo restano normali.
- `X / Y`, filtri v33.1.1, dati, backup v5, branding, icone e logiche d'asta invariati.
- Verifica runtime a 390×844 e 360×844 completata senza overflow/wrapping della barra e senza regressioni nel packing della Mappa Slot.
