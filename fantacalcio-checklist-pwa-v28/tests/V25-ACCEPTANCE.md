# V25 Acceptance

- Slot badge grayscale widened with strong-but-soft S1/S2.
- FVM size response compresses low values and expands high values.
- Main toolbar uses icon-only sort/search/filter controls.
- Search expands inline and collapses when empty/blurred.
- Filter active count remains visible as a badge.
- Service worker cache is v25.
